# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets (never commit)
.env
.env.local
.env.*.local
deploy/supabase/compose.env

# Per-machine Docker overrides (e.g. Hostinger VPS TLS mounts)
docker-compose.override.yml

# Runtime / generated
logs/*.log
upload/*
!upload/.gitkeep
media/*.pdf

# Old nginx path (migrated to docker/nginx/)
nginx/
