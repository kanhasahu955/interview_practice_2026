# mo-april content

Static Markdown under repo-root **`documents/`** is served at **`/content`** (see `ApiConstants.DOCUMENTS_MOUNT_PATH`).

Use this folder for curriculum notes, release notes, or internal copy you want browsable as raw `.md` files.
