# Copy to repo-root `.env` on your VPS (never commit real secrets).
#   cp deploy/hostinger/env.hostinger.example .env && nano .env && chmod 600 .env
#
# Full steps: deploy/hostinger/README.md
# Supabase URL shape: deploy/supabase/README.md

# --- Secrets (required) ---
JWT_SECRET_KEY=

# Your live site(s), comma-separated, no spaces. Include https://www if you use it.
CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# --- Database: pick ONE approach ---

# Option A — Postgres in Docker (service name `db` in compose)
POSTGRES_PASSWORD=
POSTGRES_USER=learn
POSTGRES_DB=moapril
DATABASE_URL=postgresql+psycopg2://learn:YOUR_POSTGRES_PASSWORD@db:5432/moapril

# Option B — Supabase (or any external Postgres): comment A, set B, then use `make prod-external-db`
# DATABASE_URL=postgresql+psycopg2://postgres:YOUR_SUPABASE_DB_PASSWORD@db.tutkjihhixijaqzhfyxk.supabase.co:5432/postgres
# (Password = Supabase Dashboard → Settings → Database. URL-encode special chars in the password.)

# --- Public branding (optional) ---
# APP_NAME=My Study Site

# --- Docker publish (only for Option A; bind Postgres to localhost on the VPS) ---
POSTGRES_PUBLISH_PORT=127.0.0.1:5432
NGINX_HTTP_PORT=80

# --- Production workers (tune to vCPU count, often 2–4 on small VPS) ---
GUNICORN_WORKERS=2
GUNICORN_TIMEOUT=120
LOG_LEVEL=INFO
RATE_LIMIT_DEFAULT=120/minute

# Optional first admin (leave empty to skip)
SEED_ADMIN_EMAIL=
SEED_ADMIN_PASSWORD=

# Disable WebSocket log stream on untrusted networks (recommended on public VPS).
LOG_WS_ENABLED=false
